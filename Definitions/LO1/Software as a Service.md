Delivers ready-to-use applications over the internet, typically through the web browser. The user doesn't need to manage any controls or any of the underlying infrastructure, platform or code. That will be handled by the service provider. The only purpose is to use the software as a simple user.