An authorization system that provides fine-grained access management for resources in Azure. You can grant the exact access that users need to do their jobs. Can grant access by assigning appropriate Azure role to users, groups, and applications at a certain scope. Roles assigned at a parent scope also grants access to the child scope contained within it. Roles assigned dictates what resources the user, group, or application can manage within that scope.